fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
pyyaml==6.0.2
httpx==0.27.2
prometheus-client==0.21.0

# OTel SDK + OTLP/HTTP exporter
opentelemetry-sdk==1.28.0
opentelemetry-exporter-otlp-proto-http==1.28.0
opentelemetry-instrumentation-fastapi==0.49b0
opentelemetry-instrumentation-httpx==0.49b0

# Sigil SDK (latest stable on PyPI as of 2026-05-22; earliest published is 0.1.2)
sigil-sdk==0.4.0
# Provider wrappers — pinned versions TBD; we currently use the raw `anthropic`
# SDK (below) + manual generation export via sigil_sdk.Client
# sigil-sdk-anthropic
# sigil-sdk-openai
# sigil-sdk-gemini

# Provider SDKs (called directly with manual Sigil generation emission;
# providers/anthropic.py is the reference Phase 1 impl. Once we verify
# sigil-sdk-anthropic at runtime we can swap to its wrapped client.)
anthropic==0.39.0
# openai==1.55.0
# google-generativeai==0.8.0
