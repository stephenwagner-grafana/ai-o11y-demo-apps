{{- if .Values.supportbot.router.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: sb-router
  namespace: support-bot
  labels:
    {{- include "aio11y.labels" "sb-router" | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "aio11y.selectorLabels" "sb-router" | nindent 4 }}
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
{{- end }}
